/** Re-exports for the workshop-manual Storage helpers (see lib/documents.ts). */
export {
  MANUAL_BUCKET,
  MANUAL_OBJECT,
  MANUAL_LOCAL_URL,
  MANUAL_SIGNED_URL_TTL,
  getDocument,
  DOCUMENTS,
  type DocumentMeta,
} from '@/lib/documents';
